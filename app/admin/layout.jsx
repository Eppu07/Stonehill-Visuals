import './admin.css';

export const metadata = {
  title: 'Admin – stonehill.architect',
  robots: { index: false, follow: false }
};

export default function AdminLayout({ children }) {
  return children;
}
